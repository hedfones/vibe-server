{
  "name": "get_product_photos",
  "description": "The function accepts a product id as an argument and returns a list of photo ids and descriptions of that product",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": ["product_id"],
    "properties": {
      "product_id": {
        "type": "integer",
        "description": "Unique identifier for the product"
      }
    },
    "additionalProperties": false
  }
}
