{
  "name": "check_availability",
  "description": "Provides associates' availability information based on location and product IDs",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": ["location_id", "product_id"],
    "properties": {
      "location_id": {
        "type": "integer",
        "description": "ID of the location"
      },
      "product_id": {
        "type": "integer",
        "description": "ID of the product"
      }
    },
    "additionalProperties": false
  }
}
